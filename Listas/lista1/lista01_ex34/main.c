#include <stdio.h>
#include <stdlib.h>

int main()
{
    int lado=0;
    printf("Insira o lado do quadrado:\n");
    scanf("%d", &lado);
    printf("A area do quadrado e %d\n", lado*lado);
    return 0;
}
