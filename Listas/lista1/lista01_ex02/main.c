#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n_cavalos=0;
    printf("Insira a quantidade de cavalos no haras:\n");
    scanf("%d", &n_cavalos);
    printf("A quantidade de ferraduras necessarias sera de %d", n_cavalos*4);
}
