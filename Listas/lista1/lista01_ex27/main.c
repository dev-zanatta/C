#include <stdio.h>
#include <stdlib.h>

int main()
{
    float a=0, b=0;
    printf("Entre com os dois numeros\n");
    scanf("%f %f", &a, &b);
    printf("A divisao do primeiro pelo segundo numero e %.2f", a/b);
    return 0;
}
